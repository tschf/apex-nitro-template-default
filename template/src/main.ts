const version = "1.0.0";

export {
  version
};

apex.jQuery(function(){
  const pageId = Number(document.getElementById("pFlowStepId").nodeValue);
	const appId = Number(document.getElementById("pFlowId").nodeValue);

  // TODO: On load page logic

});